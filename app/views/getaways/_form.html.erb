<%= form_for(@getaway) do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :days %>
  <%= f.number_field :days, min:2 %>

  <%= f.fields_for :trips do |trip| %>
    <%= trip.label :road_trip %>
    <%= trip.check_box :road_trip %>

    <%= trip.label :flight %>
    <%= trip.check_box :flight %>
 
    <%= trip.label :duration %>
    <%= trip.text_field :duration %>
  <% end %>

  <%= f.submit %>
  <% end %>

